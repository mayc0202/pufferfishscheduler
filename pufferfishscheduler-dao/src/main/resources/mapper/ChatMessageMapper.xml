<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pufferfishscheduler.dao.mapper.ChatMessageMapper">

    <resultMap id="BaseResultMap" type="com.pufferfishscheduler.dao.entity.ChatMessageEntity">
        <id column="id" property="id" jdbcType="INTEGER"/>
        <result column="session_id" property="sessionId" jdbcType="VARCHAR"/>
        <result column="message_id" property="messageId" jdbcType="VARCHAR"/>
        <result column="parent_message_id" property="parentMessageId" jdbcType="VARCHAR"/>
        <result column="role" property="role" jdbcType="VARCHAR"/>
        <result column="content_type" property="contentType" jdbcType="VARCHAR"/>
        <result column="content" property="content" jdbcType="VARCHAR"/>
        <result column="token_count" property="tokenCount" jdbcType="INTEGER"/>
        <result column="model_name" property="modelName" jdbcType="VARCHAR"/>
        <result column="metadata" property="metadata" jdbcType="VARCHAR"/>
        <result column="created_at" property="createdAt" jdbcType="TIMESTAMP"/>
    </resultMap>

    <select id="selectBySessionId" resultType="com.pufferfishscheduler.dao.entity.ChatMessageEntity">
        SELECT *
        FROM chat_message
        WHERE session_id = #{sessionId}
        ORDER BY created_at ASC LIMIT #{limit}
    </select>

    <select id="selectBySessionIdAndTime" resultType="com.pufferfishscheduler.dao.entity.ChatMessageEntity">
        SELECT *
        FROM chat_message
        WHERE session_id = #{sessionId}
          AND created_at >= #{startTime}
        ORDER BY created_at ASC
    </select>

</mapper>